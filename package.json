{
  "name": "bigquery-publicgoods-mcp-monorepo",
  "private": true,
  "description": "Monorepo for BigQuery Public Goods MCPs",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
